/** Centralized storage key constants to avoid magic strings across modules. */
export const StorageKeys = {
  RESUME_SESSION: 'uplink-resume-session',
  CACHED_MODELS: 'uplink-cached-models',
  YOLO_MODE: 'uplink-yolo',
  THEME: 'uplink-theme',
} as const;
